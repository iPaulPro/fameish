import { Tables } from "@/database.types";

export type User = Tables<"user">;

export type Winner = Tables<"winner">;

export type Score = Tables<"score">;

export type RecordCount = Tables<"record_count">;
